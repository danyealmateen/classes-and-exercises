/*

//Initera en Array
let colors = ["Red", "Green", "Yellow", "Blue"];

//Använder sig av Push() för att lägga till ett nytt element
//array.isArray(<array>) returnerar true/false beroende på om variabeln är array eller ej.
//array.shift() tar bort första elementet i en array
//array.pop() tar bort det sista elementet i en array
//array.sort()sorterar array i bokstavsordning.
colors.push("Purple");

console.log(colors);

console.log(colors[0]);

//Ändrar elementet colors
colors[1] = "Beige";

console.log(colors[1]);

console.log(`Arrayen Colors har ${colors.length} element`);
*/